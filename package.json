{
  "name": "my-sweetheart",
  "private": true,
  "type": "module",
  "description": "A one-button lyric page: a 1910 love song sung onto aged paper as a handwritten letter, every word inking in at its real sung timestamp and shedding running ink drips. Vite build (npm run dev / build / preview); tools/smoke-test-page.mjs screenshots every page.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 8080",
    "smoke:page": "node tools/smoke-test-page.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.1",
    "vite": "^8.1.3"
  }
}
