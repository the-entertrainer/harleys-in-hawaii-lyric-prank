# Attribution

## Song

**"Let Me Call You Sweetheart"** — words & music by Beth Slater Whitson and
Leo Friedman, published 1910. Both composition and lyrics are in the public
domain in the US.

**Recording used:** *The Jubileers* (vocal quartet & piano), from the series
"Let's All Howl," catalog 1801-A. Digitized 78rpm transfer, hosted on the
Internet Archive:
https://archive.org/details/78_let-me-call-you-sweetheart_the-jubileers-friedman-whitson_gbia0062289a

`audio/let-me-call-you-sweetheart.mp3` is that file, unmodified.

## Lyric timing

Every timestamp in the `LYRICS` array in `lyrics-data.mjs` — both each
line's `start`/`end` and every individual word's `s`/`e` inside its `words`
array — was produced by running
[faster-whisper](https://github.com/SYSTRAN/faster-whisper) with word-level
timestamps directly on the recording above:

```python
from faster_whisper import WhisperModel
model = WhisperModel("small", device="cpu", compute_type="int8")
segments, info = model.transcribe(
    "let-me-call-you-sweetheart.mp3", word_timestamps=True, language="en"
)
for seg in segments:
    for w in seg.words:
        print(w.start, w.end, w.word)
```

then checked against the printed 1910 lyric sheet for spelling (Whisper's
transcription is used for **timing**, not wording). One exception: the final
tag line (the last ~1.4s of the recording, a soft fade-out) came back from
Whisper with all ten words collapsed onto the same timestamp — those ten
words were evenly re-interpolated across the segment's real start/end
instead of using the (degenerate) per-word output; every other line's word
timings are Whisper's actual output, unmodified.

## Motion / choreography

Every line's and accent image's on-screen motion (position, opacity, scale,
rotation, blur) is a real [Theatre.js](https://www.theatrejs.com/) keyframe
sequence, evaluated by `@theatre/core` at runtime from
`assets/theatre-state.json`. That JSON is generated by
`tools/build-theatre-state.mjs` rather than authored by hand in Theatre.js
Studio's visual timeline editor — there's no display to run Studio's UI
against in this project's build environment, and, separately, Theatre.js
0.7.2's public `studio.transaction()` API only writes *static* prop
overrides, not sequenced keyframes, unless a prop has already been marked
"sequenced" by Studio's own right-click UI action (there is presently no
public API for that; see the still-open
[theatre-js/theatre#411](https://github.com/theatre-js/theatre/issues/411)).
Theatre.js does, however, publicly export its on-disk project-state format
as `__UNSTABLE_Project_OnDiskState`, and its own type definitions note that
format "could also be useful for users who manually edit the project state"
— so `tools/build-theatre-state.mjs` constructs that exact JSON shape
directly from the real per-line/per-word timing above. `@theatre/core` reads
and interpolates it completely normally at runtime; only the authoring step
is a script instead of mouse drags.

## GIF / PNG assets

No hand-drawn or procedurally generated illustration is used anywhere in
this build — every decorative image is a real file sourced from the web, for
personal/non-commercial use on this page.

### Animated GIFs — `assets/gif/`

All seven are transparent-background sticker GIFs from [GIPHY](https://giphy.com)'s
sticker search (GIPHY's stickers are provided for this kind of personal,
non-commercial embedding):

| File | What it is | Used for |
|---|---|---|
| `heart-beat.gif` | A beating heart | The end-of-song finale reveal |
| `rose-bloom.gif` | A red rose blooming | Chorus line accent |
| `flower-bud-bloom.gif` | A flower bud opening | Chorus line accent |
| `doves-flying.gif` | Two white doves in flight | Verse line accent ("moonlight gleams... in dreams") |
| `cherry-blossom-garland.gif` | A cherry-blossom branch garland | Chorus (reprise) accent |
| `sparkle-twinkle.gif` | Twinkling sparkle particles | Ambient floater during an instrumental gap |
| `vinyl-record.gif` | A spinning vinyl record / turntable | Background ambience during the spoken intro banter |

### Transparent PNGs — `assets/png/`

All eleven are [OpenMoji](https://openmoji.org) emoji renders (color, 618×618),
licensed [CC BY-SA 4.0](https://creativecommons.org/licenses/by-sa/4.0/) —
free for any use, including commercial, with attribution, which this file
provides:

| File | Emoji | Used for |
|---|---|---|
| `rose.png` | 🌹 U+1F339 | Chorus accent |
| `bouquet.png` | 💐 U+1F490 | Ambient floater |
| `two-hearts.png` | 💕 U+1F495 | Verse accent |
| `sparkling-heart.png` | 💖 U+1F496 | Chorus accent |
| `hibiscus.png` | 🌺 U+1F33A | Verse accent |
| `sun.png` | ☀️ U+2600 | Verse accent ("keep the love light glowing") |
| `crescent-moon.png` | 🌙 U+1F319 | Ambient floater |
| `musical-notes.png` | 🎵 U+1F3B6 | Outro accent / ambient floater |
| `ribbon.png` | 🎀 U+1F380 | Finale-adjacent line accent |
| `sparkles.png` | ✨ U+2728 | Ambient floater |

Downloaded from `raw.githubusercontent.com/hfg-gmuend/openmoji/master/color/618x618/<codepoint>.png`
and verified to have genuine alpha-channel transparency (not a white/solid
background) before use.

## Fonts

Fraunces and Inter — both via Google Fonts (SIL Open Font License).
